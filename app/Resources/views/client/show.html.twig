{% extends 'base.html.twig' %}

{% block body %}
    <h1>Client</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Clid</th>
                <td>{{ client.clId }}</td>
            </tr>
            <tr>
                <th>Clname</th>
                <td>{{ client.clName }}</td>
            </tr>
            <tr>
                <th>Clespece</th>
                <td>{{ client.clEspece }}</td>
            </tr>
            <tr>
                <th>Cdname</th>
                <td>{{ client.cdName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('client_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('client_edit', { 'id': client.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
